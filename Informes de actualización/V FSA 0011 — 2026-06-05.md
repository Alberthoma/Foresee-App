# Informe de Actualización — V FSA 0011
**Fecha:** 2026-06-05

---

## Solicitud del usuario

Eliminar la línea de borde horizontal visible en la parte inferior del tab-bar y encima del action-bar.

---

## Modificaciones realizadas

### 1. CSS — `#tab-bar` (`index.html`)

```css
/* Eliminado */
border-bottom: 1px solid var(--color-border);
```

### 2. CSS — `#action-bar` (`index.html`)

```css
/* Eliminado */
border-bottom: 1px solid var(--color-border);
```

Ambas líneas separadoras han sido removidas. La transición visual entre tab-bar, action-bar y contenido ahora es limpia y sin bordes.
